# LLM

A shared client abstraction over the official `openai`/`anthropic` SDKs, used
by the Milestone 5 prompting-based linkers to call a local Ollama model in
dev or a frontier API in production through the same interface.

::: linkingtk.llm.client
